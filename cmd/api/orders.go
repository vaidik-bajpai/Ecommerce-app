package main

/* func (app *application) placeOrder(w http.ResponseWriter, r *http.Request) {
	v := validator.NewValidator()
	userId := app.readQueryInt(r.URL.Query(), "user_id", v)
	if !v.Valid() {
		app.failedValidationResponse(w, r, v.Errors)
		return
	}

} */
